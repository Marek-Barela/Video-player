import { Box } from '@chakra-ui/react';
import Navigation from 'components/Navigation';

const HomeView = () => {
  return (
    <Box minH={'100vh'}>
      <Navigation />
    </Box>
  );
};

export default HomeView;
